// Escreva uma função que receba a idade de uma pessoa em anos e retorne a mesma idade em dias.
function emDias(idade) {
    return `Uma pessoa com ${idade} anos já viveu ${idade * 365} dias.`; 
}

console.log(emDias(22));